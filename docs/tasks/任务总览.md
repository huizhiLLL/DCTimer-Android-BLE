# DCTimer-Android 任务总览

日期：`2026-04-17`

## 文档说明

- 这份文档用于汇总当前任务进度，替代原先分散在 `tasks/` 下的多份阶段拆分文档。
- 已完成任务会按类型做结论级总结。
- 当前仍需继续推进或验证的事项，会单独列为待办，不和历史完成项混写。

## 一、已完成任务总结

### 1. Android 底座升级

已完成内容：

- [x] 升级到 `AndroidX`
- [x] 升级到 `AGP 8.9.2 + Gradle 8.11.1 + JDK 17`
- [x] 提升 `compileSdk / targetSdk` 到 `35`
- [x] 切换数据库导入导出到系统文档选择器
- [x] 切换打乱导入导出到系统文档选择器
- [x] 切换背景图选择到 `ACTION_OPEN_DOCUMENT` 并持久化 `Uri`
- [x] 保留旧 `picPath` 作为背景图兼容回退
- [x] 移除旧 `FileSelectorDialog` 与相关旧文件主流程
- [x] 移除主线 APK 自更新入口、安装权限与 `FileProvider`
- [x] 在 JDK 17 下通过 `assembleDebug`
- [x] 在 JDK 17 下通过 `assembleRelease`

已完成的回归与修正：

- [x] Android 12 真机已回归数据库导入导出、打乱导入导出、背景图重启后保持
- [x] “计时页左下角统计信息消失”已确认属于设置项关闭，不是布局缺陷
- [x] 已保留应用窗口不覆盖系统导航栏的修正
- [x] 已恢复 BLE 扫描的 `ACCESS_FINE_LOCATION` 兼容权限申请

### 2. 智能魔方蓝牙计时主线

已完成内容：

- [x] `MoYu32` 智能魔方协议主链可用
- [x] `QiYi` 智能魔方协议主链可用
- [x] `Tornado V4 智能版` 已复用 `QiYi` 协议链完成兼容
- [x] `GAN v2 / v3 / v4` 已接入统一智能魔方主链
- [x] `MoYu32 / QiYi / GAN` 已统一收口到 `SmartCubeProtocol`
- [x] BLE 扫描弹窗已改为直接列出设备并自动识别协议
- [x] 智能魔方支持状态同步、电量同步、转动上报
- [x] 已打通“按打乱进入 `READY` -> 首转起表 -> 还原停表 -> 自动保存成绩”
- [x] 已保存智能魔方成绩步序 `moves`
- [x] 已补普通 `3x3` 打乱目标状态自动推导
- [x] 状态弹窗和计时页右下角已切换到智能魔方 3D 状态图
- [x] 已支持单步转层补间动画
- [x] 已提供“重置魔方”入口
- [x] 打乱公式区域已支持进度渐隐/变色提示
- [x] 偏离打乱时已支持“最短纠错路径 + 剩余打乱”提示
- [x] 已按增量推进方式收敛纠错提示闪烁问题
- [x] 已修正智能魔方完成复原后直接保存并进入下一轮
- [x] 默认界面已调整为纯白背景 + 纯黑文字

### 3. 蓝牙设备分层与蓝牙计时器接入

已完成内容：

- [x] `enterTime` 已拆成“智能魔方 / 蓝牙计时器”两条独立硬件入口
- [x] `APP.readPref()` 中旧的计时方式回退逻辑已修正
- [x] 旧 `SmartTimer.java` 已删除
- [x] 旧 `BLEDevice.TYPE_GAN_TIMER` 已删除
- [x] `BluetoothTools` 已增加 `SmartTimerProtocol` 分发支路
- [x] `QiYi Smart Timer` 已通过独立 `QiyiSmartTimerProtocol` 接入
- [x] 已补 `QiYi Smart Timer` 的 hello、finish ACK、状态映射与默认 MAC 兜底
- [x] 蓝牙扫描弹窗标题已按当前模式区分为“智能魔方 / 蓝牙计时器”
- [x] 设置页中的原“蓝牙设备”入口已改为“智能魔方”，并新增“蓝牙计时器”
- [x] `assembleDebug` 已通过构建验证

### 4. Release 打包配置

已完成内容：

- [x] `app/build.gradle` 已接入标准 release 签名入口
- [x] release 构建支持通过项目根目录 `key.properties` 注入签名
- [x] release 构建支持通过 `-P` 参数临时传入签名信息
- [x] 未提供签名信息时会直接报错，不再生成不可分发包
- [x] 版本号已升级为 `versionCode 200 / versionName 2.0 Beta`
- [x] 已新增 `key.properties.example`
- [x] 已使用显式签名参数完成 `assembleRelease` 验证
- [x] 已新增 `rel.bat` 快捷脚本
- [x] release APK 输出名已固定为 `DCTimer-BLE-v版本号.apk`

## 二、当前仍需继续推进的任务

### 1. 智能魔方主线待补项

- [ ] Android 15 真机继续回归 BLE 扫描、重扫、连接、断开与自动计时主链
- [ ] 连接失败、解密失败、MAC 无效等异常提示与恢复流程补齐
- [ ] 手动输入 MAC 的正式 UI 兜底落地
- [ ] 继续细调 3D 状态图、打乱进度提示和纠错提示体验

### 2. 蓝牙计时器主线待补项

- [ ] 真机回归验证 `QiYi Smart Timer` 的 `READY / RUNNING / STOPPED` 主链
- [ ] 验证蓝牙计时器断开、重连、异常恢复时主页面状态
- [ ] 根据真机结果判断是否继续扩更多蓝牙计时器协议

### 3. 整体验证待补项

- [ ] Android 12 / 15 真机继续回归 BLE 扫描、连接、断开与自动计时
- [ ] 继续确认智能魔方与蓝牙计时器两条主链互不污染

## 三、当前不再单独保留的旧任务主题

以下任务已经并入本总览，不再继续保留独立任务文档：

- `MoYu32` 智能魔方计时一期任务拆分
- `Android` 底座升级任务拆分
- `Release` 打包配置任务

## 四、后续维护建议

- 新任务优先直接补进这份总览，不再为同一主题重复新建任务文档。
- 如果后续有真正独立的新模块，例如“联机房间对战正式开发”，再考虑单独新建任务文档。
- 已完成且没有后续动作的事项，保留结论级摘要即可，不再保留长篇过程记录。
